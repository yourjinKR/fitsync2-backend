<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="fitsync"/>

    <!-- 로그 경로 -->
    <property name="LOG_PATH" value="logs"/>

    <!-- 텍스트 로그 파일 -->
    <property name="APP_LOG_FILE" value="${LOG_PATH}/app.log"/>
    <property name="ERROR_LOG_FILE" value="${LOG_PATH}/error.log"/>
    <property name="HTTP_LOG_FILE" value="${LOG_PATH}/http.log"/>

    <!-- JSON 로그 파일 -->
    <property name="APP_JSON_LOG_FILE" value="${LOG_PATH}/app-json.log"/>
    <property name="HTTP_JSON_LOG_FILE" value="${LOG_PATH}/http-json.log"/>

    <!-- 패턴 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [requestId=%X{requestId:-}] - %msg%n"/>

    <property name="FILE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [requestId=%X{requestId:-}] - %msg%n"/>

    <!-- =========================
         CONSOLE
    ========================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- =========================
         APP FILE (일반 텍스트 로그)
    ========================== -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_LOG_FILE}</file>

        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- =========================
         ERROR FILE
    ========================== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${ERROR_LOG_FILE}</file>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>

        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- =========================
         HTTP FILE (RequestLoggingFilter 전용 텍스트 로그)
    ========================== -->
    <appender name="HTTP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${HTTP_LOG_FILE}</file>

        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/http.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- =========================
         APP JSON FILE (전체 JSON 로그)
    ========================== -->
    <appender name="APP_JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_JSON_LOG_FILE}</file>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"appName":"${APP_NAME}","logCategory":"application"}</customFields>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/app-json.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- =========================
         HTTP JSON FILE (RequestLoggingFilter 전용 JSON 로그)
    ========================== -->
    <appender name="HTTP_JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${HTTP_JSON_LOG_FILE}</file>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"appName":"${APP_NAME}","logCategory":"http"}</customFields>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/http-json.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- =========================
         HTTP LOGGER
         RequestLoggingFilter 로그만 별도 파일/JSON으로 관리
    ========================== -->
    <logger name="app.fitsync.global.logging.RequestLoggingFilter" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="HTTP_FILE"/>
        <appender-ref ref="HTTP_JSON_FILE"/>
    </logger>

    <!-- =========================
         ROOT LOGGER
    ========================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
        <appender-ref ref="APP_JSON_FILE"/>
    </root>

</configuration>